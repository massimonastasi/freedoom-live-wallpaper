<?xml version="1.0" encoding="utf-8"?>
<!--
    One Material 3 list item.

    Material 3 has no List or ListItem widget for Views - it is a specification, not a
    component, unlike Compose where ListItem exists. So this is the one thing on the screen
    that has to be assembled by hand, and everything inside it is a real M3 widget.

    Measured off the design's own item rather than estimated: on a 340dp row the content
    starts at 16 and is 244 wide, and the trailing element starts at 272 - so 12dp between
    the text and the control, and the text sits 10dp from the top and bottom of a 64dp row.

    minHeight rather than a fixed height, because two lines of bodyLarge plus bodyMedium is
    taller than 64 and a row that clips its own text is worse than one that grows.

    Pulled in with <include> wherever a row is needed, so there is one row in the project and
    the sections cannot drift apart.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/list_row_background"
    android:gravity="center_vertical"
    android:minHeight="64dp"
    android:orientation="horizontal"
    android:paddingStart="16dp"
    android:paddingEnd="16dp">

    <com.google.android.material.radiobutton.MaterialRadioButton
        android:id="@+id/row_radio"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:clickable="false"
        android:duplicateParentState="true"
        android:focusable="false" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="12dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingTop="10dp"
        android:paddingBottom="10dp">

        <TextView
            android:id="@+id/row_label"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAppearance="?attr/textAppearanceBodyLarge"
            android:textColor="?attr/colorOnSurface"
            tools:text="Dungeon floor" />

        <TextView
            android:id="@+id/row_caption"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAppearance="?attr/textAppearanceBodyMedium"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:visibility="gone"
            tools:text="Changes with the difficulty"
            tools:visibility="visible" />

        <!-- Anything the row carries beyond its text: the colour swatches live in theirs. -->
        <FrameLayout
            android:id="@+id/row_extra"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:visibility="gone" />

    </LinearLayout>

    <com.google.android.material.materialswitch.MaterialSwitch
        android:id="@+id/row_switch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:clickable="false"
        android:focusable="false"
        android:visibility="gone" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/row_action"
        style="@style/Widget.Material3.Button.IconButton.Filled.Tonal"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:contentDescription="@null"
        android:insetTop="0dp"
        android:insetBottom="0dp"
        android:visibility="gone"
        app:iconPadding="0dp"
        app:iconSize="20dp" />

</LinearLayout>
