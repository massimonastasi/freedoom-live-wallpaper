<?xml version="1.0" encoding="utf-8"?>
<!--
    The container for a list of option rows.

    The wrapper is not decoration: PreferenceViewHolder looks its ids up beneath the row's
    view, so a container used as the layout root is not found and the bind crashes - measured
    once already, on the radio list.

    16dp inset on the group and a 2dp gap between rows, which is what makes a "segmented"
    list read as separate filled shapes rather than one block.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:id="@+id/option_rows"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp" />

</FrameLayout>
