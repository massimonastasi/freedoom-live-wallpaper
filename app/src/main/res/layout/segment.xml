<?xml version="1.0" encoding="utf-8"?>
<!--
    One segment. Equal weight so the three share the width evenly, which is what makes the
    control read as one object rather than three buttons of different sizes.

    A layout rather than a constructor call: a style is applied to a view when it is
    inflated, and passing a style resource to a widget's three-argument constructor sets its
    default style attribute instead - not the same thing, and it leaves the button unstyled.
-->
<com.google.android.material.button.MaterialButton
    xmlns:android="http://schemas.android.com/apk/res/android"
    style="?attr/materialButtonOutlinedStyle"
    android:layout_width="0dp"
    android:layout_height="match_parent"
    android:layout_weight="1"
    android:insetTop="0dp"
    android:insetBottom="0dp" />
