<?xml version="1.0" encoding="utf-8"?>
<!--
    The chevron the accordion rows carry, rotated 180 degrees when open. A vector rather than
    a font glyph or a bitmap: it tints with the theme, so it follows dynamic colour without a
    second copy for dark mode.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="?attr/colorOnSurfaceVariant">
    <path
        android:fillColor="@android:color/white"
        android:pathData="M12,15.4L6.6,10l1.4,-1.4l4,4l4,-4L17.4,10z" />
</vector>
